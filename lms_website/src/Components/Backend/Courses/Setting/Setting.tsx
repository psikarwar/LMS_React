import React from 'react';
import SettingContent from './SettingContent/SettingContent';  // Adjust the import path if necessary

const Setting: React.FC = () => {
  return (
    <div>
      <SettingContent />
    </div>
  );
};

export default Setting;
